# Python
__pycache__
*.py[cod]
*.egg-info
.eggs
*.egg
venv/
.venv/
env/
*.log
.mypy_cache/
.pytest_cache/
.python-version

# Git
.git
.gitignore

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Project
chroma_data/
outputs/
uploads/
templates/

# 环境变量文件——密钥不进镜像。compose 是通过 environment: 注入的，
# 镜像里不需要它。本地 backend/.env 存在，不排除会被 COPY . . 带进去。
.env

# 服务器上遗留、不属于 git 的探针与备份：它们会被 COPY . . 带进镜像，
# 使镜像里出现 git 没有的文件（2026-09-23 审计出容器里就有这 6 个）。
/_bak_*/
/_probe_*.py
**/*.bak*

# Tests
tests/
test_*.py
